opt_in_rules:
  - force_unwrapping

# Personalização de regras com severidade de warning
force_cast:
  severity: warning

excluded:
  - Pods
  - Carthage

disabled_rules:
  - force_try
  - identifier_name
  - line_length
  - force_cast
  - multiple_closures_with_trailing_closure